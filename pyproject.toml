[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "landlockpy"
dynamic = ["version"]
description = "Python bindings for the Landlock Linux security module"
readme = "README.md"
requires-python = ">=3.10"
license = "0BSD"
authors = [{ name = "Quad4" }]
keywords = ["landlock", "lsm", "sandbox", "security", "linux"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: BSD License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Security",
    "Topic :: System :: Operating System Kernels :: Linux",
    "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://quad4.io"
Repository = "https://github.com/Quad4-Software/landlockpy"
Documentation = "https://docs.kernel.org/userspace-api/landlock.html"
Issues = "https://github.com/Quad4-Software/landlockpy/issues"
Changelog = "https://github.com/Quad4-Software/landlockpy/blob/master/CHANGELOG.md"

[tool.hatch.version]
path = "src/landlockpy/__init__.py"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "hypothesis>=6.168.0",
    "ruff>=0.16.8",
    "bandit>=1.9.4",
    "mypy>=2.3.1",
]

[tool.ruff]
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "S", "PTH", "C90", "RUF", "TRY"]
ignore = ["TRY003"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "S603"]

[tool.mypy]
strict = true
files = ["src", "tests"]

[tool.bandit]
exclude_dirs = ["tests"]

[tool.coverage.run]
branch = true
source = ["src/landlockpy"]

[tool.coverage.report]
show_missing = true
fail_under = 80

[tool.pytest.ini_options]
testpaths = ["tests"]
xfail_strict = true
filterwarnings = ["error"]
markers = [
    "enforced: tests that apply a Landlock ruleset in a child process",
]
